#
#   Module for xfClock - GestureRecognizer
#
import xfClock.module


class Weather(xfClock.module.moduleBase):
    
    def on_init(self):

        pass       

    def on_render(self):
        #lock.aquire()
        #try:
        #    if self.gesture <> "":
        #        self.lastGesture = self.gesture
        #finally:
        #    lock.release()
        pass